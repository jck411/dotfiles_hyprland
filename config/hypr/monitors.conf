# Monitor layout for external displays
# Managed by nwg-displays or edited manually.
# Built-in display (eDP-1) is configured per-host in hosts/*.conf.
# External monitors added below (e.g., by nwg-displays):
